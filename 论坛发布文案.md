# 一个10MB的程序，让整个局域网变成KTV

## 你有没有想过——

公司年会、部门团建、学校晚会、单位联欢……

每次想唱KTV都要订包厢？花钱不说，还得等位、排队、凑时间。

**如果你们有局域网，那其实你已经有了KTV。**

---

## 它是什么

一个10MB的Windows程序，放在有歌曲的电脑上运行，整个局域网立刻变身KTV点歌系统。

- 电脑接电视/投影 → 播放屏
- 手机扫码 → 点歌遥控器

就这样，没了。

**客户端零依赖——有浏览器就行，不需要安装任何APP、任何插件、任何运行时。** 电脑用Chrome/Edge，手机用微信扫码或浏览器打开，开箱即用。

---

## 真的只要10MB

没有安装包，没有依赖项，没有运行时。就一个exe文件，加上ffmpeg（也是两个exe），总共不到100MB，放在U盘里都能带着走。

Go语言编写，所有前端代码内嵌，零外部文件依赖。支持32位和64位系统，从Win7到Win11全覆盖。

---

## 怎么用

**30秒上手：**

1. 把程序放到歌曲文件夹所在的电脑上
2. 双击运行，在菜单里填入歌曲目录（支持多个盘符，H盘F盘J盘随便挂）
3. 浏览器打开显示的地址 — 那就是点歌界面
4. 手机扫页面上的二维码 — 那就是遥控器

**然后呢？** 搜歌、点歌、切歌、原唱伴奏切换，跟你在外面K房做的操作一模一样。

**打开页面就能看到系统状态** — FFmpeg有没有装好、GPU加速有没有生效、曲库目录是否正常，一目了然。有问题直接告诉你怎么解决，不用瞎猜。

---

## 功能速览

**点歌方式：**
- 关键词搜索，16万首曲库10毫秒出结果
- 支持歌名+歌手组合搜索，拼音首字母搜索（输入"zgr"即可找到"张国荣"）
- 歌手分类浏览，按首字母快速定位
- 语种分类（国语/粤语/英语/日语/韩语...）
- 曲种分类（流行歌曲/经典老歌/摇滚/民谣...）
- 随机点歌，适合选择困难症
- 同名歌曲自动标注文件大小，让你选画质更好的版本

**播放：**
- 视频、音频自动识别
- 原唱/伴奏一键切换
- 内嵌歌词实时滚动
- Ctrl+↓快捷键切歌
- 音频可视化：歌词/波形/频谱条/频谱曲线 四种模式
- 频谱颜色自定义，峰值着色开关
- 频率刻度实时标注
- 曲库文件异常自动检测：无画面/无声音的文件会显著标红提醒，告诉你这不是系统问题

**手机遥控：**
- 扫码即用，不用装APP，浏览器打开就能用
- 搜索、点歌、切歌全支持
- 可设密码，防止陌生人乱点歌
- 多人同时扫码，各点各的，队列共享

**省流模式：**
- 一键开启，视频实时转码为低码率流媒体
- 手机看MV只需500kbps，比原文件省90%流量
- GPU全流水线加速（解码+编码都在GPU），1-2秒即可出画面
- 动态流控，只缓冲15秒，切歌不浪费带宽

**歌曲上传：**
- 网页拖拽上传，批量添加
- 上传后自动增量扫描，秒级更新曲库
- 支持mp4/mkv/avi/mp3/flac等18种格式

**曲库缓存：**
- 首次扫描后自动保存缓存，下次启动可选快速加载
- 读取缓存秒级启动，无需重新扫描

**系统自检：**
- 启动时自动检测FFmpeg、FFprobe、GPU加速、曲库目录
- 打开页面即显示系统状态总览，有问题红色标注并附解决方案
- 曲库目录不存在或为空？一键打开设置修改

**其他：**
- 缺歌登记，大家想唱什么一目了然
- 自动记录搜不到的关键词，方便补曲库
- 支持HEVC/H.265自动转码，不挑编码格式
- NVIDIA/AMD/Intel GPU自动检测，硬件加速转码
- 同时提供32位和64位版本，Win7 SP1也能跑

---

## 适合什么场景

**学校** — 机房、多媒体教室、宿舍局域网，晚自习后同学一起唱

**公司** — 会议室接投影仪，年会部门聚会不用订包厢

**单位** — 文体活动室，午休放松一下

**集团内网** — 多人共享曲库，跨楼层跨部门同唱

**家里** — HTPC接电视，家庭KTV，不用买点歌机

---

## 技术亮点

（给喜欢折腾的朋友看）

- Go语言编写，单文件部署，前端全部内嵌
- 客户端零依赖，纯浏览器即可使用，无需安装任何软件
- Fragmented MP4实时流媒体，FFmpeg管道输出
- GPU全流水线加速（CUDA解码+NVENC编码），无GPU自动回退CPU
- 启动自检系统：FFmpeg/FFprobe/GPU/曲库目录逐一检测，异常状态页面显著提示
- 媒体轨道检测：自动发现无视频/无音频的异常文件并标注
- 曲库缓存双区分离（固定目录+上传目录），增量扫描毫秒级
- 曲库本地缓存文件，二次启动秒级加载
- 预计算搜索索引+拼音首字母，16万首关键词搜索<10ms
- Web Audio API实时可视化（歌词/波形/频谱条/频谱曲线），预分配缓冲零GC
- QR中继服务器架构，支持跨网段扫码点歌
- 原生支持Lyrics3和ID3v2 USLT歌词提取
- 32位/64位双版本，兼容Windows 7 SP1到Windows 11

---

## 你需要的

- 一台Windows电脑（Win7 SP1及以上，存放歌曲+运行程序）
- 局域网环境
- ffmpeg.exe + ffprobe.exe（放程序同目录）
- 歌曲文件（你自己的曲库，越多越好）

**客户端？不需要。** 有浏览器就行——电脑的、手机的、平板的，打开网页就能用。

---

## 获取方式

开源免费，欢迎自建曲库，欢迎分享给同事同学。

曲库大小不限——实测16万首（450G+），搜索依然10毫秒出结果。

---

*10MB的程序，16万首的歌，整栋楼都是你的K房。*
