# v1.5.0 大更新

## 🆕 新功能

### 二维码服务器内置
- **内置扫码点歌**：二维码服务器已集成进主程序，与主服务器**共用同一个 IP 和端口**，局域网内扫码即可点歌，无需再单独运行 qrserver 程序。
- **设置面板新增「二维码服务器类型」**：内置（推荐）/ 外接，并附带详细的适用场景与操作说明。
  - 内置：适合手机与点歌电脑在同一局域网内，扫码即可访问；共用主服务器 IP 和端口。
  - 外接：适合公网/异地扫码点歌，需自行部署独立 qrserver 并保证手机能访问到（公网 IP / 端口映射 / 内网穿透）。
- **移动端点歌页面已内置进 exe**，单个程序即可完整运行（无需单独携带页面文件）。
- **内置模式队列实时同步**：手机点歌 / 切歌 / 删除后，手机端队列即时刷新，无需再手动刷新页面。

### 音频可视化性能优化
- 大幅降低波形 / 频谱可视化对 GPU / 和 CPU 的占用（画布分辨率上限、ResizeObserver 缓存尺寸、避免每帧触发布局等）。
- 播放窗口左上角新增**实时 FPS 帧率显示**，按帧率区间着色（≥50 帧绿色 / 30-49 帧黄色 / <30 帧红色），便于观察不同窗口/全屏模式的性能表现。

## 🐛 修复

- 修复**内置扫码模式下，手机端队列里看不到已点歌曲**的问题（队列更新未实时推送）。
- 修复音频可视化 requestAnimationFrame 叠加导致的**帧率虚高**（120/240/360fps）与资源浪费。
- 仅有一条音轨的曲库文件的**强提示改为只在播放前 10 秒显示**，之后自动消失，不再常驻页面顶部。

## 📦 下载
- 单个 exe 即可运行（内置扫码页面、二维码服务器均已内置）。
- 另附独立 `qrserver.exe`，供外接 / 公网扫码点歌场景部署使用。