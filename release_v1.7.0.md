# v1.7.0 更新说明

## 新增：歌词在线搜索（7 大音源聚合）
- 本地无 `.lrc`、且文件内也无内嵌歌词时，自动联网搜索歌词并保存为歌曲同名 `.lrc`，命中即停、不再重复下载
- 聚合 **7 个免费音源**，按序接入：QQ 音乐 → 网易云音乐 → LRCLIB → 酷狗 → 酷我 → 咪咕 → lrc.cx
- 搜索结果来源标识精确到接口（如「QQ 音乐」「网易云」「本地文件」），不再笼统显示「在线搜索」

## 新增：歌词匹配逻辑优化
- 匹配优先级调整为 **时长匹配 → 歌名相似度 → 歌手相似度**，降低误配率
- 歌名自动剥离 `(live)` / `(DJ)` / `[xx]` 等括号尾缀并归一化，无全字匹配时按 LCS 相似度自动选取最高命中（<0.35 判未命中）

## 新增：歌词时间偏移微调
- 播放页新增 **[提前] / ±偏移量s / [推后]** 三连按钮，每次点击整体提前/推后 0.5 秒
- 按钮中间实时显示累计偏移量，手动纠正「歌词对但整体错位」的歌词
- 切换歌曲时偏移量自动复位为 0

## 新增：手动选择歌词
- 播放页新增「手动选词」按钮，弹出候选歌词列表（含各接口、歌名、歌手、时长）
- 弹窗自动识别并预填歌名 / 歌手，可手动修改后再点「搜索歌词」
- 选定后立即保存本地，并以手动选择内容**强制覆盖**同名 `.lrc` 为准

## 新增：文件名解析增强
- 支持「序号.歌名.dts」等仅有歌名（无歌手分隔符）的文件名格式

## 新增：音频 / 视频格式全兼容
- 覆盖 ffmpeg 常见容器格式：`.dts` `.dff` `.dsf` `.wma` `mp4` `mkv` `m2ts` `mxf` `wtv` 等，转码、曲库扫描、上传白名单、播放识别全线打通

## 新增：实时转码码率可配置
- 设置页新增「音频实时转码码率」项，范围 32–512kbps，默认 192kbps

## 修复与优化
- **视频文件不再处理歌词**（mp4 / mkv / m2ts 等直接隐藏歌词容器，不触发在线搜索）
- 歌词来源提示显示在左上角，3 秒后自动淡出，切换歌曲时自动清除避免重叠
- 歌词字体大小自适应（延迟计算，避免容器未可见时宽度为 0）

---
**下载**：附件中的 `ktv.exe`（64位）/ `ktv-32bit.exe`（32位），扫码服务器 `qrserver.exe` / `qrserver-32bit.exe`。