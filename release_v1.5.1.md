# v1.5.1 修正版

## 🐛 修复

- **修复：自动切歌后频谱条/波形可视化偶发空白显示**
  - 根因：上一首自然播完时 `audio.onended` 会停止绘制循环（`stopAnim`），但切到下一首时没有重启 `drawFrame` 绘制循环，导致分析器虽已重连、画布却一直空白；手动切换视图才能恢复。
  - 修复：`playAudio` 中按当前视图重启可视化绘制循环，自动切歌后波形、频谱条、频谱曲线均恢复正常显示。

## 📦 下载
- 与 v1.5.0 一致，4 个 exe：`ktv.exe` / `ktv-32bit.exe` / `qrserver.exe` / `qrserver-32bit.exe`。