# v1.9.0 更新说明

## 新增

- 手机端点播页音频播放新增**歌词显示**：与电脑端一致，本地 .lrc → 缺失时自动联网搜索，并支持手动搜索/选歌词（复用 /api/lyrics 系列接口）

- 手机端点播页音频播放新增**实时电平表**（L/R 双声道 LED）：WebAudio ScriptProcessor 采集采样，模拟专业调音台表头

- 电平表采用调音台风格：信号上升立即跟随、信号回落每 50ms 掉 1 格、峰值标记亮橙色填充并保持 0.5 秒

- 电平表量程 -30dB ~ 0dB，带 -30/-20/-10/0 刻度

- 浏览器劫持自动降级：夸克/部分自带浏览器会接管网络流媒体导致 analyser 无数据，自动改为 blob 缓存播放并持久化记录，后续播放直接走缓存免等待（X 浏览器/Chrome 不受影响）

## 修复

- 修复手机端歌词查找传参错误（剥扩展名导致 findMediaFile 404），与桌面端参数对齐

- 修复手机端电平表在夸克/自带浏览器无信号问题（媒体就绪后才绑定 createMediaElementSource + 浏览器劫持检测降级）
